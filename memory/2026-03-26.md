# 2026-03-26

- 修复 `entry/src/main/ets/runtime/DemoRuntime.ets` 的 ArkTS 编译错误：
  - `queryLogs` 查询参数从 `limit` 改为 `length`（与 `LogQueryOptions` 定义一致）。
  - 为查询对象补充显式类型 `LogQueryOptions`，消除 “Object literal must correspond to explicitly declared class or interface” 报错。
- 补充 TDD 校验：
  - `scripts/verify-demo-entry.mjs` 新增断言，要求 DemoRuntime 使用 `length: 6`，并禁止 `limit: 6`。
  - 先触发失败（红灯）后修复通过（绿灯）。
- 结果：
  - `./scripts/build-demo-entry.sh` 构建成功。
  - `./scripts/start-demo-via-hdc.sh --no-build` 安装并启动成功，`EntryAbility` 进入 `FOREGROUND`。
  - 最新 HAP 时间戳：`2026-03-26 18:32:00`。
- 视图树采集从“source 占位树”切换为“真实 FrameNode 树”：`ExportServer` 新增 `setViewTreeSnapshotProvider`，由 DemoRuntime 注入 `UIContext + FrameNode` 递归遍历逻辑。
- Demo 页根节点增加固定 id：`neptune-demo-root`，`/v2/ui-tree/snapshot` 通过 `getFrameNodeById` 获取根节点并递归输出 `id/parentId/name/children`。
- 实测网关透传返回真实节点：`root_count=1`、`node_count=9`、`first_root_name=Column`，不再返回仅 2 条 source 根节点。
- 组件树采集改为无手动锚点模式：不再依赖 `.id('neptune-demo-root')` 与 `getFrameNodeById`，改用页面组件 `this.getUniqueId()` 自动登记 anchor，并优先走 `UIContext.getFilteredInspectorTree(['isLayoutInspector'])` 解析。
- 采集降级策略：Inspector JSON 解析失败时，回退到 `getFrameNodeByUniqueId + getParent + getChild(ExpandMode.EXPAND)` 的 FrameNode 递归遍历。
- 真机模拟器验证：`/v2/ui-tree/snapshot` 从 `node_count=9` 提升到 `node_count=72`，根节点 `name=Scroll`，与页面结构一致性显著提升。
- 视图树协议升级到 V2：`ExportViewTreeNode` 新增 `frame/style/text/visible` 可选字段。
- DemoRuntime 已接入 inspector 布局字段提取逻辑（`$rect` 解析 + FrameNode 几何兜底），并修复 ArkTS 对动态索引访问的编译限制。
- 当前实测现状：网关快照仍无 `frame`（`has_frame_any=0`），后续需继续验证 ArkUI inspector 输出字段与几何 API 取值口径。

## ArkUI Inspector JSON 导出接口
- 新增路由：`GET /v2/ui-tree/inspector`，用于返回原始 ArkUI Inspector JSON。
- `ExportServer` 增加 `InspectorSnapshotProvider` 扩展点与 `inspectorSnapshot()` 默认实现。
- `DemoRuntime` 注入 `setInspectorSnapshotProvider`，通过 `uiContext.getFilteredInspectorTree(['content'])` 返回 payload。
- 返回结构：`snapshotId/capturedAt/platform/available/payload/reason`。
- 真机联调验证：`http://127.0.0.1:28767/v2/ui-tree/inspector` 返回 200，`available=true`，含完整 payload。
- 调整 `/v2/ui-tree/inspector` 响应格式：`payload` 从 JSON 字符串改为 JSON 对象（不可用时为 `null`），并在 ExportServer 路由层增加字符串到对象的兼容解析。
