# 2026-03-24

## Harmony 最小工程骨架

- 为 `neptune-sdk-harmony` 补齐了 Harmony 项目壳：`build-profile.json5`、`hvigorfile.ts`、`hvigor/hvigor-config.json5`、`AppScope/app.json5`。
- 新增 `library/` HAR 模块配置，命令形态为 `./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 顶层 `src/main/ets` 继续作为唯一源码来源；新增 `scripts/sync-harmony-module.sh`，在构建前同步到 `library/src/main/ets`。
- 本机验证通过：`./hvigorw --mode project tasks --no-daemon`、`./hvigorw --mode project clean --no-daemon`。

## Harmony 构建阻塞清理

- 新增仓库级 `.ohpmrc`，将 `ohpm` 源固定为 `https://ohpm.openharmony.cn/ohpm/`，避免命中用户态的 `ohpm.dxy.net` 私有源。
- 本机验证通过：`ohpm install --all`。
- `RdbLogStore` 已切换到 API 12 可识别的 `@kit.ArkData` 命名导入：`relationalStore`。
- 已清理 ArkTS 阻塞构建的语法：index signature、对象 spread、`any`/索引访问类型、内联对象类型声明。
- 本机验证通过：`./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 行为回归脚本通过：`node ./scripts/verify-source-dedup.mjs`、`node ./scripts/verify-log-query-filtering.mjs`、`node ./scripts/verify-log-persistence.mjs`。

## CI 补齐

- 已新增 `.github/workflows/ci.yml`，默认只在 `push(main)` / `pull_request` 上跑 runner-safe 校验：shell 语法检查 + Node 回归脚本。
- `workflow_dispatch` 通过 `run_harmony_build=true` 显式开启时，才会尝试执行 `ohpm install --all` 和 `./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 默认 CI 路径已明确规避 DevEco 依赖，避免在标准 GitHub runner 上因缺少 Harmony 环境而失败。

## Demo 冒烟链路

- 新增 `scripts/demo-smoke.mjs`，以本地参考导出服务模拟 `/v2/export/*` 链路，注入 demo 日志后输出摘要并断言 `health`、`metrics`、`logs`、`sources` 的关键字段。
- `README.md` 已补充 demo smoke 运行说明，`.github/workflows/ci.yml` 已把 smoke 纳入 runner-safe 校验。
- 这次 smoke 明确不依赖 Harmony 运行时，目的是先把接入级导出契约和快速回归链路固定下来。

## Harmony Demo App

- 新增可直接上模拟器的 `entry/` HAP Demo App，按钮触发 `demoRuntime.seedBatch()`，并通过本地 `ExportServer + LogQueue` 刷新 metrics / sources / recent logs 摘要。
- `entry` 通过 `oh-package.json5` 引用本地 `library` HAR，不复制核心 SDK 代码。
- 新增独立构建命令 `./hvigorw --mode module -p module=entry assembleHap --no-daemon`，以及包装脚本 `scripts/build-demo-entry.sh`。
- `AppScope/app.json5` 已补齐 `icon` / `label`，`entry/src/main/module.json5` 已补齐 `startWindowIcon` / `startWindowBackground`，并在 `build-profile.json5` 打开 `buildOption.strictMode.useNormalizedOHMUrl=true`，解决 `@cxy/webserver` 的 bytecode HAR 约束。
- 本机验证通过：`./hvigorw --mode project tasks --no-daemon`、`node ./scripts/verify-demo-entry.mjs`、`./hvigorw --mode module -p module=entry assembleHap --no-daemon`、`./scripts/build-demo-entry.sh`。
- README 与 dev/feature 文档已补充 DevEco Studio / `hdc` 的模拟器运行步骤。

## Harmony 网关发现

- 新增 `src/main/ets/discovery/`：`GatewayDiscoveryResolver`、`GatewayDiscoveryMdnsProvider`、`GatewayDiscoveryHttpClient`、错误与模型定义。
- discovery 策略为 `mDNS` 候选优先，随后回退手动 `DSN`；默认 `mDNS` 提供器是空实现，避免把系统级能力写死。
- 新增 `scripts/verify-gateway-discovery.mjs`，覆盖 `mDNS` 命中、`DSN` 回退、非法响应跳过、全失败错误四个场景。
- `README.md` 已补充 discovery 用法与当前能力边界。

## Harmony Demo discovery 接入

- `entry/src/main/ets/runtime/DemoRuntime.ets` 已新增网关发现状态和 resolver 接线，默认手动 `DSN` 回退路径为 `127.0.0.1:18765`。
- `entry/src/main/ets/pages/Index.ets` 已新增“发现网关”按钮和结果卡片，页面可直接展示 `source/host/port/version/endpoint` 或失败原因。
- `scripts/verify-gateway-discovery.mjs` 已补充成功与失败两条展示路径的纯脚本断言。
- 本机验证通过：`node ./scripts/verify-gateway-discovery.mjs`、`./hvigorw --mode module -p module=entry assembleHap --no-daemon`。

## Harmony WS 客户端链路

- 新增 `src/main/ets/ws/`：`GatewayWsClient`、`GatewayWsManager`、`GatewayWsModels`。
- WS 链路使用 `@kit.NetworkKit` 的 `webSocket.createWebSocket()`，启动后直连 `/v2/ws`，建连后发送 `hello(role=sdk)`。
- 心跳与失联策略固定为 `15s` / `45s`，重连退避为 `0.5s / 1s / 2s / 4s / 8s`，收到 `command.dispatch(ping)` 时回 `command.ack`。
- discovery / 手动 DSN 变更会驱动 WS 端点更新并重连；`DemoRuntime` 已在启动时后台开启 WS 管理器。
- 新增 `scripts/verify-gateway-ws-contract.mjs`，并把 `scripts/verify-demo-entry.mjs` 一并补上 WS 启动接线断言。
- 本机验证通过：`node ./scripts/verify-gateway-ws-contract.mjs`、`./hvigorw --mode project tasks --no-daemon`、`./hvigorw --mode module -p module=library assembleHar --no-daemon`、`./hvigorw --mode module -p module=entry assembleHap --no-daemon`。

## Harmony mDNS 实装（NetworkKit）

- `GatewayDiscoveryMdnsProvider` 已从空实现升级为 `NetworkKitGatewayDiscoveryMdnsProvider`，实际调用 `@kit.NetworkKit` 的 `mdns.createDiscoveryService` + `resolveLocalService`，并输出 `source=mdns` 的候选。
- `GatewayDiscoveryConfig` 新增 `mdnsContext`、`mdnsServiceType`、`mdnsServiceName`；默认 service type 为 `_neptune._tcp`，并兼容传入带 `.local` 的格式。
- `GatewayDiscoveryResolver` 默认 provider 已切换到 `NetworkKitGatewayDiscoveryMdnsProvider`；当 `mdnsContext` 未提供时自动返回空候选并回退 manual DSN，不会破坏旧链路。
- `GatewayWsManager` 的 discovery 配置合并已补齐 `mdnsContext` / `mdnsServiceType` / `mdnsServiceName`，后续重连会带上同一 discovery 约束。
- Demo 侧已在 `Index.aboutToAppear` 注入 `UIAbilityContext` 到 runtime，发现链路和 WS manager 都能共享 mDNS 上下文。
- 本机验证通过：`node ./scripts/verify-gateway-discovery.mjs`、`node ./scripts/verify-gateway-ws-contract.mjs`、`./hvigorw --mode module -p module=library assembleHar --no-daemon`、`./hvigorw --mode module -p module=entry assembleHap --no-daemon`。
