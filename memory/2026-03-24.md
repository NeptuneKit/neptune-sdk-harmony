# 2026-03-24

## Harmony 最小工程骨架

- 为 `neptune-sdk-harmony` 补齐了 Harmony 项目壳：`build-profile.json5`、`hvigorfile.ts`、`hvigor/hvigor-config.json5`、`AppScope/app.json5`。
- 新增 `library/` HAR 模块配置，命令形态为 `./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 顶层 `src/main/ets` 继续作为唯一源码来源；新增 `scripts/sync-harmony-module.sh`，在构建前同步到 `library/src/main/ets`。
- 本机验证通过：`./hvigorw --mode project tasks --no-daemon`、`./hvigorw --mode project clean --no-daemon`。

## Harmony 构建阻塞清理

- 新增仓库级 `.ohpmrc`，将 `ohpm` 源固定为 `https://ohpm.openharmony.cn/ohpm/`，避免命中用户态的 `ohpm.dxy.net` 私有源。
- 本机验证通过：`ohpm install --all`。
- `RdbLogStore` 已切换到 API 12 可识别的 `@kit.ArkData` 命名导入：`relationalStore`。
- 已清理 ArkTS 阻塞构建的语法：index signature、对象 spread、`any`/索引访问类型、内联对象类型声明。
- 本机验证通过：`./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 行为回归脚本通过：`node ./scripts/verify-source-dedup.mjs`、`node ./scripts/verify-log-query-filtering.mjs`、`node ./scripts/verify-log-persistence.mjs`。

## CI 补齐

- 已新增 `.github/workflows/ci.yml`，默认只在 `push(main)` / `pull_request` 上跑 runner-safe 校验：shell 语法检查 + Node 回归脚本。
- `workflow_dispatch` 通过 `run_harmony_build=true` 显式开启时，才会尝试执行 `ohpm install --all` 和 `./hvigorw --mode module -p module=library assembleHar --no-daemon`。
- 默认 CI 路径已明确规避 DevEco 依赖，避免在标准 GitHub runner 上因缺少 Harmony 环境而失败。

## Demo 冒烟链路

- 新增 `scripts/demo-smoke.mjs`，以本地参考导出服务模拟 `/v2/export/*` 链路，注入 demo 日志后输出摘要并断言 `health`、`metrics`、`logs`、`sources` 的关键字段。
- `README.md` 已补充 demo smoke 运行说明，`.github/workflows/ci.yml` 已把 smoke 纳入 runner-safe 校验。
- 这次 smoke 明确不依赖 Harmony 运行时，目的是先把接入级导出契约和快速回归链路固定下来。
